<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="mapper.myroom">
    
    <!-- member login -->
	<select id="selectAll" parameterType="java.lang.String" resultType="com.hotel.vo.MyroomVO">
		select member.mid,basket.bid,book.radatestart,book.radateend,book.price,basket.brname from member,book,basket
		where member.mid = book.mid and book.bid = basket.bid and member.mid=#{mid}
	</select>
    
    </mapper>